apiVersion: v1
kind: Pod
metadata:
  name: my-nginx-pod
  labels:
    app: my_ak8_lebel
spec:
  containers:
    - name: my-nginx-pod
      image: nginx
      ports:
        - containerPort: 80
